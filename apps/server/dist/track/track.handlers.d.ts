import type { WebSocket } from "ws";
/**
 * Handle incoming WebSocket messages from the widget.
 * All messages are validated with Zod before processing.
 */
export declare function handleTrackMessage(ws: WebSocket, data: unknown): void;
//# sourceMappingURL=track.handlers.d.ts.map