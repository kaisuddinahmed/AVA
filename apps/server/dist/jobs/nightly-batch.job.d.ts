import type { NightlyBatchResult } from "@ava/shared";
/**
 * Run the full nightly batch. Each subtask runs independently;
 * a failure in one does not block the others.
 */
export declare function runNightlyBatch(): Promise<NightlyBatchResult>;
//# sourceMappingURL=nightly-batch.job.d.ts.map